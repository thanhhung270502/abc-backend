name: Node.js CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    container: node:16
    steps:
      - uses: actions/checkout@v4
      - run: npm ci
      - run: node src/index.js
